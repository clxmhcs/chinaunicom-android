package com.clxmhcs.chinaunicom.core.design

import androidx.compose.ui.graphics.Color

object ChinaUnicomColors {
    // RootView.swift: RGB (0.18, 0.40, 0.95)
    val BrandBlue = Color(0xFF2E66F2)

    // DashboardView.swift: RGB (0.95, 0.97, 1.0)
    val FlowDashboardTop = Color(0xFFF2F7FF)

    // Provisional Android mappings for iOS system semantic colors.
    // Exact light/dark tuning remains gated on the deferred M7 screenshots.
    val LightBackground = Color(0xFFF2F2F7)
    val LightSurface = Color(0xFFFFFFFF)
    val LightPrimaryText = Color(0xFF111111)
    val LightSecondaryText = Color(0x99111111)

    val DarkBackground = Color(0xFF000000)
    val DarkSurface = Color(0xFF1C1C1E)
    val DarkPrimaryText = Color(0xFFF5F5F7)
    val DarkSecondaryText = Color(0x99F5F5F7)
}
