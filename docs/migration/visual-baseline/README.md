# iOS Visual Screenshot Baseline

This directory is reserved for real screenshots from the frozen iOS source baseline.

Required filenames and capture rules are defined in `../M0_VISUAL_BASELINE.md`.

Do not place screenshots containing unmasked passwords, Cookie values, `token_online`, SMS/captcha codes or identity-card data in Git.
